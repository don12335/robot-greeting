<!DOCTYPE html>
<html lang="zh-Hant">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>機器人打招呼（輸入名字）</title>
  <style>
    body {
      background-color: #121212;
      color: #f4ede3;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      text-align: center;
      padding: 2rem;
    }
    input, button {
      font-size: 1.2rem;
      padding: 0.6rem 1rem;
      border-radius: 8px;
      margin-top: 1rem;
    }
    input {
      width: 250px;
      border: 1px solid #444;
      background: #2c2c2c;
      color: #fff;
    }
    button {
      background: linear-gradient(to right, #d97706, #fca311);
      color: white;
      border: none;
      cursor: pointer;
    }
    #greeting {
      margin-top: 2rem;
      font-size: 1.5rem;
      color: #fca311;
      font-weight: bold;
    }
  </style>
</head>
<body>
  <h1>我是 don 的機器人 🤖</h1>
  <p>輸入你的名字，看看我會怎麼跟你打招呼吧！</p>
  <input type="text" id="nameInput" placeholder="請輸入你的名字" />
  <br />
  <button onclick="sayHi()">打招呼</button>
  <div id="greeting"></div>

  <script>
    function sayHi() {
      const name = document.getElementById("nameInput").value.trim();
      const greetingDiv = document.getElementById("greeting");

      if (!name) {
        greetingDiv.textContent = "請先輸入你的名字喔！😊";
        return;
      }

      const greetings = [
        `嗨，${name}！很高興見到你！🤖`,
        `你好，${name}！我是你的機器人朋友！👋`,
        `歡迎光臨，${name}！讓我們一起探索吧！✨`,
        `你好啊，${name}！有什麼我可以幫忙的嗎？😊`,
        機器人打招呼囉，${name}！祝你今天愉快！🚀
      ];

      const randomGreeting = greetings[Math.floor(Math.random() * greetings.length)];
      greetingDiv.textContent = randomGreeting;
    }
  </script>
</body>
</html>
